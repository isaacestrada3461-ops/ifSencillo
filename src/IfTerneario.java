import java.util.Scanner;

public class IfTerneario {
    static void main(String[] args) {
        Scanner leer = new Scanner(Sistem.in)
        //Instancia de calsesina scanner para usar metodos
        // como ustedes lo usan ;
        String password = leer.nextLine();
        String buenaContraseña = password.length() 8

        
        Scanner leer = new Scanner(Sistem.in)
        String tipo = leer.nextLine();
        String serie =(tipo.equals("anime")) ;
    }
}
